import React from 'react';

export class Tab extends React.Component<any, any> {
  state = {
    name: '',
  };

  render() {
    const { tab } = this.props;

    return (
      <div className="tab">
        
      </div>
    );
  }
}